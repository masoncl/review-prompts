# CL-005: Memory ordering for lockless access

**Risk**: Data corruption

**When to check**: Mandatory when shared fields accessed without locks

Place each step defined below into TodoWrite.

**Mandatory memory ordering validation:**
- step 1: use READ_ONCE/WRITE_ONCE for shared fields accessed without locks

**After analysis:** Issues found: [none OR list]
