# CL-007: Cleanup ordering

**Risk**: Use-after-free

**When to check**: Mandatory when resources are cleaned up

Place each step defined below into TodoWrite.

**Mandatory cleanup ordering validation:**
- step 1: verify cleanup follows proper ordering: Stop users → wait completion → destroy resources

**After analysis:** Issues found: [none OR list]
